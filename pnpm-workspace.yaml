packages:
  - "."
  - "apps/*"
# Dependencies whose install/build scripts are allowed to run (pnpm v11 moved
# this out of package.json's "pnpm.onlyBuiltDependencies").
onlyBuiltDependencies:
  - esbuild
  - sharp
